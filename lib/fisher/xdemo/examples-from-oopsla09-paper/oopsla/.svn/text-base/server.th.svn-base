spawn server {
  var datesvc;
  sync dateServicePlz() = datesvc;
  sync registerDateSvc(datesvc') { 
     datesvc := datesvc'; 
  }
body{
  println("Hi! I'm the server!  Are you the server, too?");
  while(true){
    serve;
    println("Server: I just did something. Did you?");
  }
}body
}server;
